The packages below are the itinerary AFTER the organizer's edit. Each one lists its current description,
its days with the current summary, and its items as "- SLOT activityId name". The REWRITE line of a
package says exactly which texts to return for it; return nothing else.

{packages}
